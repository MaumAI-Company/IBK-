<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.ibk.repository.BatchInferenceModelRepository">
    <select id="countByTarget" resultType="int">
        SELECT COUNT(*)
        FROM BATCH_INFERENCE_MODEL bim
        WHERE bim.TARGET = #{target}
    </select>

    <update id="update">
        UPDATE BATCH_INFERENCE_MODEL
        SET MODEL_ID = #{modelId, jdbcType=INTEGER},
            MOD_ID   = #{modId},
            MOD_DT   = NOW()
        WHERE TARGET = #{target}
    </update>

    <insert id="insert">
        INSERT INTO BATCH_INFERENCE_MODEL
            (TARGET, MODEL_ID, MOD_ID, MOD_DT)
        VALUES (#{target}, #{modelId, jdbcType=INTEGER}, #{modId}, NOW())
    </insert>

    <delete id="deleteByTarget">
        DELETE
        FROM BATCH_INFERENCE_MODEL
        WHERE TARGET = #{target}
    </delete>

    <select id="findByTarget" resultType="BatchInferenceModelInfo">
        SELECT *
        FROM BATCH_INFERENCE_MODEL
        WHERE TARGET = #{target}
    </select>

    <delete id="deleteByModelId">
        DELETE
        FROM BATCH_INFERENCE_MODEL
        WHERE MODEL_ID = #{modelId}
    </delete>

</mapper>